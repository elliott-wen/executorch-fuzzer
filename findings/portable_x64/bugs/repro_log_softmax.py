#!/usr/bin/env python3
"""Repro: portable-kernel bug `log_softmax` (x86 host ExecuTorch runtime via xnnpack_client).
_log_softmax on a non-finite input returns 0.0 where ATen returns NaN.
Single-op graph from corpus job w1:188, lowered to the PORTABLE backend and run on the
connected worker. Prints eager (ATen) vs device (portable kernel). SKIP/CRASH → status.
Needs a connected worker on the broker (BROKER_JOB_PORT, default 15564).
"""
import os, sys, time, warnings, logging
os.environ.setdefault("CUDA_VISIBLE_DEVICES", "")
warnings.filterwarnings("ignore"); logging.disable(logging.WARNING)
sys.path.insert(0, "/data/jwen929")
import torch, zmq
from mobile.gen.export import build_job
from mobile.executor import protocol as P, compare as cmp
JOB = 'w1:188'
PORT = int(os.environ.get("BROKER_JOB_PORT", "15564"))
SRC = '# delegated: backend=portable ops=0 non_delegated=2 delegate_calls=0\n"""Auto-generated by mobile — graph definition.\nGraph: n0*=_log_softmax.out(L0,·,·,·)\nRun via the mobile ExecuTorch fuzzer (xnnpack_client / ...).\n"""\nimport torch\ntorch.manual_seed(12648430)  # reproducible leaf data\n\ndef _make(shape, dtype):\n    if dtype.is_floating_point or dtype.is_complex:\n        return torch.randn(shape, dtype=dtype)\n    if dtype == torch.bool:\n        return torch.randint(0, 2, shape, dtype=dtype)\n    lo = -4 if dtype.is_signed else 0  # unsigned dtypes reject a negative low\n    return torch.randint(lo, 5, shape, dtype=dtype)\n\n_NONFIN_P = 0.05  # single-op domain-fuzz probability\ndef _inject(t):\n    # exercise non-finite / boundary INPUT domains (NaN/inf/-inf/0/-0).\n    if not t.dtype.is_floating_point or t.numel() == 0:\n        return t  # nan/inf only meaningful for real floats\n    if torch.rand(1).item() >= _NONFIN_P:\n        return t  # leave this leaf clean\n    choices = [float(\'nan\'), float(\'inf\'), float(\'-inf\'), 0.0, -0.0]\n    v = choices[int(torch.randint(len(choices), (1,)).item())]\n    mask = torch.rand(t.shape) < 0.5\n    mask.reshape(-1)[0] = True  # guarantee >=1 injected element\n    return torch.where(mask, torch.full_like(t, v), t)\n\ndef _first(r):\n    if isinstance(r, torch.Tensor): return r\n    if isinstance(r, (tuple, list)):\n        for x in r:\n            if isinstance(x, torch.Tensor): return x\n    return r\n\nL0 = _inject(_make((), torch.float32))\n\ndef g(L0):\n    n0 = _first(torch.ops.aten._log_softmax.out(L0, 0, False, out=torch.empty((), dtype=torch.float32)))\n    return (n0,)\n\nLEAVES = [L0]'
job = build_job(SRC, "portable", False)
if job.status != "READY":
    print(f"[{JOB}] build_job {job.status}: {job.detail[:160]}"); sys.exit(0)
ctx = zmq.Context.instance(); D = ctx.socket(zmq.DEALER)
D.setsockopt(zmq.HEARTBEAT_IVL, 5000); D.connect(f"tcp://127.0.0.1:{PORT}")
PL = zmq.Poller(); PL.register(D, zmq.POLLIN)
fr = P.encode_pushjob(f"repro::log_softmax", job.pte, job.inputs, job.eager, desc="repro", user_pos=job.user_pos)
D.send_multipart(P.job_frames_from_pushjob(fr))
t0 = time.monotonic()
while time.monotonic() - t0 < 60:
    if dict(PL.poll(1000)).get(D):
        _, st, det, raw = P.decode_result(D.recv_multipart())
        if st != "RAN":
            print(f"[{JOB}] device status: {st}  {det[:160]}"); break
        et = cmp.select(raw, job.user_pos)
        if len(et) > len(job.eager): et = et[len(et)-len(job.eager):]
        status, detail = cmp._cmp(job.eager[0], et[0], 0)
        print(f"[{JOB}] log_softmax -> {status}  {detail}")
        print("  eager :", job.eager[0].flatten()[:8].tolist())
        print("  device:", et[0].flatten()[:8].tolist())
        break
