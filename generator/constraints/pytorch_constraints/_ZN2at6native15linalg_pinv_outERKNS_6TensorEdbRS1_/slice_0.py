"""Combined + Z3-optimized constraints for _ZN2at6native15linalg_pinv_outERKNS_6TensorEdbRS1_.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

from z3 import *
from mobile.generator.constraints.model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar, MAX_DIM


def _pinv_out_ok(out, self):
    # pinv output = self with the last two dims transposed (shape [..., n, m]
    # for input [..., m, n]).  Either empty, or exactly that shape.
    nd = self.ndim
    shape_ok = And(
        out.ndim == nd,
        Select(out.sizes, nd - IntVal(1)) == Select(self.sizes, nd - IntVal(2)),
        Select(out.sizes, nd - IntVal(2)) == Select(self.sizes, nd - IntVal(1)),
        *[Implies(IntVal(i) < nd - IntVal(2),
                  Select(out.sizes, IntVal(i)) == Select(self.sizes, IntVal(i)))
          for i in range(MAX_DIM)]
    )
    return Or(out.numel() == IntVal(0), shape_ok)

ENTRY = '_ZN2at6native15linalg_pinv_outERKNS_6TensorEdbRS1_ (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('self', 'Tensor'), ('rcond', 'float'), ('hermitian', 'bool'), ('out', 'Tensor')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    out_ok = _pinv_out_ok(TensorVar('out'), TensorVar('self'))
    return [
        Not(And(Or((Int('self.dtype') == IntVal(6)), (Int('self.dtype') == IntVal(7)), (Int('self.dtype') == IntVal(9)), (Int('self.dtype') == IntVal(10))), (Int('self.ndim') >= IntVal(2)), Or(Not(Bool('hermitian')), (Select(Array('self.sizes', IntSort(), IntSort()), (IntVal(-1) + Int('self.ndim'))) == Select(Array('self.sizes', IntSort(), IntSort()), (IntVal(-2) + Int('self.ndim'))))), (Int('out.dtype') == Int('self.dtype')), out_ok)),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    pass
    return {}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
