"""Combined + Z3-optimized constraints for _ZN2at6native24structured_linalg_lu_out4implERKNS_6TensorEbS4_S4_S4_.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

from z3 import *
from mobile.generator.constraints.model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar, MAX_DIM


def _batch_matrix_ok(out, A, d_m, d_n):
    """out is empty OR out has A's batch dims with last two dims (d_m, d_n)."""
    conds = [out.ndim == A.ndim]
    for i in range(MAX_DIM):
        conds.append(Implies(IntVal(i) < A.ndim - IntVal(2),
                             out.size(i) == A.size(i)))
    conds.append(out.size(A.ndim - IntVal(2)) == d_m)
    conds.append(out.size(A.ndim - IntVal(1)) == d_n)
    return Or(out.numel() == 0, And(*conds))

ENTRY = '_ZN2at6native24structured_linalg_lu_out4implERKNS_6TensorEbS4_S4_S4_ (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('A', 'Tensor'), ('pivot', 'bool'), ('P', 'Tensor'), ('L', 'Tensor'), ('U', 'Tensor')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    A = TensorVar('A')
    P = TensorVar('P')
    L = TensorVar('L')
    U = TensorVar('U')
    m = A.size(A.ndim - IntVal(2))
    n = A.size(A.ndim - IntVal(1))
    k = If(m < n, m, n)
    # P:[...,m,m], L:[...,m,k], U:[...,k,n]
    out_ok_P = _batch_matrix_ok(P, A, m, m)
    out_ok_L = _batch_matrix_ok(L, A, m, k)
    out_ok_U = _batch_matrix_ok(U, A, k, n)
    return [
        Not(And((Int('P.dtype') == Int('A.dtype')), (Int('L.dtype') == Int('A.dtype')), (Int('U.dtype') == Int('A.dtype')), (IntVal(2) <= Int('A.ndim')), Bool('pivot'), Or((IntVal(6) == Int('A.dtype')), (IntVal(7) == Int('A.dtype')), (IntVal(9) == Int('A.dtype')), (IntVal(10) == Int('A.dtype'))), out_ok_P, out_ok_L, out_ok_U)),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    A = TensorVar('A')
    P = TensorVar('P')
    L = TensorVar('L')
    U = TensorVar('U')
    return {'A': A, 'P': P, 'L': L, 'U': U}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
