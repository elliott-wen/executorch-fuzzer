"""Combined + Z3-optimized constraints for _ZN2at6native30_embedding_bag_backward_symintERKNS_6TensorES3_S3_S3_S3_S3_N3c106SymIntEblbRKSt8optionalIS1_El.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

from z3 import *
from mobile.generator.constraints.model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar

ENTRY = '_ZN2at6native30_embedding_bag_backward_symintERKNS_6TensorES3_S3_S3_S3_S3_N3c106SymIntEblbRKSt8optionalIS1_El (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('grad', 'Tensor'), ('indices', 'Tensor'), ('offsets', 'Tensor'), ('offset2bag', 'Tensor'), ('bag_size', 'Tensor'), ('maximum_indices', 'Tensor'), ('num_weights', 'int'), ('scale_grad_by_freq', 'bool'), ('mode', 'int'), ('sparse', 'bool'), ('per_sample_weights', 'Tensor?'), ('padding_idx', 'int')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    return [
        Not(And(Or((IntVal(3) == Int('offsets.dtype')), (IntVal(4) == Int('offsets.dtype'))), (Int('offsets.dtype') == Int('indices.dtype')), (Int('offset2bag.dtype') == Int('indices.dtype')), (Int('bag_size.dtype') == Int('indices.dtype')), Or((IntVal(5) == Int('grad.dtype')), (IntVal(15) == Int('grad.dtype')), (IntVal(6) == Int('grad.dtype')), (IntVal(7) == Int('grad.dtype'))), (IntVal(2) == Int('grad.ndim')), Not(Bool('sparse')), (IntVal(0) == Int('mode')), Not(Bool('per_sample_weights.present')), (IntVal(1) == Int('indices.ndim')), (IntVal(1) == Int('offset2bag.ndim')), (Select(Array('offset2bag.sizes', IntSort(), IntSort()), IntVal(0)) == Select(Array('indices.sizes', IntSort(), IntSort()), IntVal(0))), (IntVal(1) <= Int('num_weights')), (Real('indices.data_lo') >= RealVal(0)), (Real('indices.data_hi') <= (RealVal(-1) + ToReal(Int('num_weights')))), (Real('offset2bag.data_lo') >= RealVal(0)), (Real('offset2bag.data_hi') <= (RealVal(-1) + ToReal(Select(Array('grad.sizes', IntSort(), IntSort()), IntVal(0))))), (IntVal(1) <= Select(Array('grad.sizes', IntSort(), IntSort()), IntVal(0))))),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    grad = TensorVar('grad')
    indices = TensorVar('indices')
    offsets = TensorVar('offsets')
    offset2bag = TensorVar('offset2bag')
    bag_size = TensorVar('bag_size')
    maximum_indices = TensorVar('maximum_indices')
    per_sample_weights = OptVar('per_sample_weights', TensorVar('per_sample_weights' + '.val'))
    return {'grad': grad, 'indices': indices, 'offsets': offsets, 'offset2bag': offset2bag, 'bag_size': bag_size, 'maximum_indices': maximum_indices, 'per_sample_weights': per_sample_weights}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
