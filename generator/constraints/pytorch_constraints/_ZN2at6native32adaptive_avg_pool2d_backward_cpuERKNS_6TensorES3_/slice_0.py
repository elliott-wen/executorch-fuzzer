"""Combined + Z3-optimized constraints for _ZN2at6native32adaptive_avg_pool2d_backward_cpuERKNS_6TensorES3_.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

from z3 import *
from mobile.generator.constraints.model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar

ENTRY = '_ZN2at6native32adaptive_avg_pool2d_backward_cpuERKNS_6TensorES3_ (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('grad_output', 'Tensor'), ('self', 'Tensor')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    return [
        Not(And(Or((Int('grad_output.ndim') == IntVal(3)), (Int('grad_output.ndim') == IntVal(4))), (Int('self.ndim') == Int('grad_output.ndim')), Or((Int('grad_output.dtype') == IntVal(5)), (Int('grad_output.dtype') == IntVal(15)), (Int('grad_output.dtype') == IntVal(6)), (Int('grad_output.dtype') == IntVal(7))), (Int('self.dtype') == Int('grad_output.dtype')), Or((Int('grad_output.ndim') <= IntVal(1)), Not((Select(Array('grad_output.sizes', IntSort(), IntSort()), IntVal(1)) <= IntVal(0)))), Or((Int('grad_output.ndim') <= IntVal(2)), Not((Select(Array('grad_output.sizes', IntSort(), IntSort()), IntVal(2)) <= IntVal(0)))), Or((Int('grad_output.ndim') <= IntVal(3)), Not((Select(Array('grad_output.sizes', IntSort(), IntSort()), IntVal(3)) <= IntVal(0)))), Or((Int('grad_output.ndim') <= IntVal(4)), Not((Select(Array('grad_output.sizes', IntSort(), IntSort()), IntVal(4)) <= IntVal(0)))))),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    grad_output = TensorVar('grad_output')
    self = TensorVar('self')
    return {'grad_output': grad_output, 'self': self}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
