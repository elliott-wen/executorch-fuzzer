"""Combined + Z3-optimized constraints for _ZN2at6native27structured__linalg_eigh_out4implERKNS_6TensorESt17basic_string_viewIcSt11char_traitsIcEEbS4_S4_.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

from z3 import *
from mobile.generator.constraints.model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar, MAX_DIM


def _out_ok(out, A, tail):
    """out is empty OR out == (A batch dims [:-2]) ++ tail (list of Z3 exprs)."""
    nb = A.ndim - IntVal(2)
    conds = [out.ndim == nb + IntVal(len(tail))]
    for i in range(MAX_DIM):
        conds.append(Implies(IntVal(i) < nb, out.size(i) == A.size(i)))
    for j, t in enumerate(tail):
        conds.append(out.size(nb + IntVal(j)) == t)
    return Or(out.numel() == 0, And(*conds))

ENTRY = '_ZN2at6native27structured__linalg_eigh_out4implERKNS_6TensorESt17basic_string_viewIcSt11char_traitsIcEEbS4_S4_ (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('A', 'Tensor'), ('UPLO', 'str'), ('compute_v', 'bool'), ('eigenvalues', 'Tensor'), ('eigenvectors', 'Tensor')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    A = TensorVar('A')
    eigenvalues = TensorVar('eigenvalues')
    eigenvectors = TensorVar('eigenvectors')
    compute_v = Bool('compute_v')
    m = A.size(A.ndim - IntVal(2))
    n = A.size(A.ndim - IntVal(1))  # == m
    out_ok_vals = _out_ok(eigenvalues, A, [m])  # [batch, m]
    out_ok_vecs = If(compute_v, _out_ok(eigenvectors, A, [m, n]),
                     eigenvectors.numel() == 0)
    return [
        Not(And((Int('A.ndim') >= IntVal(2)), (Select(Array('A.sizes', IntSort(), IntSort()), (IntVal(-1) + Int('A.ndim'))) == Select(Array('A.sizes', IntSort(), IntSort()), (IntVal(-2) + Int('A.ndim')))), Or((IntVal(6) == Int('A.dtype')), (IntVal(7) == Int('A.dtype')), (IntVal(9) == Int('A.dtype')), (IntVal(10) == Int('A.dtype'))), (Int('eigenvalues.dtype') == If((IntVal(9) == Int('A.dtype')), IntVal(6), If((IntVal(10) == Int('A.dtype')), IntVal(7), Int('A.dtype')))), (Int('eigenvectors.dtype') == Int('A.dtype')), out_ok_vals, out_ok_vecs)),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    A = TensorVar('A')
    eigenvalues = TensorVar('eigenvalues')
    eigenvectors = TensorVar('eigenvectors')
    return {'A': A, 'eigenvalues': eigenvalues, 'eigenvectors': eigenvectors}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
