"""Combined + Z3-optimized constraints for _ZN2at6native12linspace_outERKNS_6TensorERKN3c106ScalarElRS1_.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

from z3 import *
from mobile.generator.constraints.model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar

ENTRY = '_ZN2at6native12linspace_outERKNS_6TensorERKN3c106ScalarElRS1_ (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('start', 'Tensor'), ('end', 'Scalar'), ('steps', 'int'), ('out', 'Tensor')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    # linspace casts start + i*(end-start)/(steps-1) into `out`.  A complex
    # start tensor (or complex `end` scalar) carries a non-zero imaginary part
    # that cannot be cast into a non-complex `out` ("value cannot be converted
    # to type <T> without overflow"); so a complex endpoint forces complex out.
    # A non-finite (nan/inf) `end` scalar likewise cannot be cast into an
    # integer/bool `out`, so restrict it to a floating/complex out there.
    _start = TensorVar('start')
    _end = ScalarVar('end')
    _out = TensorVar('out')
    _complex_link = Implies(Or(_start.is_complex(), _end.is_complex()), _out.is_complex())
    _finite_link = Implies(Or(_end.is_nan, _end.is_inf),
                           Or(_out.is_floating_point(), _out.is_complex()))
    return [
        Not(And(_complex_link, _finite_link, Not((IntVal(11) == Int('out.dtype'))), Not((IntVal(8) == Int('out.dtype'))), Not((IntVal(23) == Int('out.dtype'))), Not((IntVal(24) == Int('out.dtype'))), Not((IntVal(25) == Int('out.dtype'))), Not((IntVal(26) == Int('out.dtype'))), Not((IntVal(44) == Int('out.dtype'))), Not((IntVal(27) == Int('out.dtype'))), Not((IntVal(28) == Int('out.dtype'))), Not((IntVal(29) == Int('out.dtype'))), (Int('start.ndim') == IntVal(0)), (IntVal(0) <= Int('steps')))),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    start = TensorVar('start')
    end = ScalarVar('end')
    out = TensorVar('out')
    return {'start': start, 'end': end, 'out': out}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
