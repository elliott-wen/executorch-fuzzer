"""Combined + Z3-optimized constraints for _ZN2at6native15_pdist_backwardERKNS_6TensorES3_dS3_.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

from z3 import *
from mobile.generator.constraints.model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar

ENTRY = '_ZN2at6native15_pdist_backwardERKNS_6TensorES3_dS3_ (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('grad', 'Tensor'), ('self', 'Tensor'), ('p', 'float'), ('pdist', 'Tensor')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    return [
        Not(And(Not(And(Not((IntVal(12) == Int('self.device_type'))), Not((IntVal(1) == Int('self.device_type'))), Not((IntVal(0) == Int('self.device_type'))), Not((IntVal(20) == Int('self.device_type'))))), Not(And(Or((IntVal(12) == Int('self.device_type')), (IntVal(1) == Int('self.device_type')), (IntVal(0) == Int('self.device_type')), (IntVal(20) == Int('self.device_type'))), Not((IntVal(7) == Int('self.dtype'))), Not((IntVal(6) == Int('self.dtype'))))))),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    grad = TensorVar('grad')
    self = TensorVar('self')
    pdist = TensorVar('pdist')
    return {'grad': grad, 'self': self, 'pdist': pdist}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
