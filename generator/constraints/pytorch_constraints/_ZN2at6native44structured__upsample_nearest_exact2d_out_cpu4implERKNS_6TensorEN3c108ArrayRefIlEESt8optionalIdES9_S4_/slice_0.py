"""Combined + Z3-optimized constraints for _ZN2at6native44structured__upsample_nearest_exact2d_out_cpu4implERKNS_6TensorEN3c108ArrayRefIlEESt8optionalIdES9_S4_.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

from z3 import *
from mobile.generator.constraints.model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar, same_shape, out_is_broadcast

ENTRY = '_ZN2at6native44structured__upsample_nearest_exact2d_out_cpu4implERKNS_6TensorEN3c108ArrayRefIlEESt8optionalIdES9_S4_ (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('self', 'Tensor'), ('output_size', 'int[]'), ('scales_h', 'float?'), ('scales_w', 'float?'), ('out', 'Tensor')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    out = TensorVar('out')
    self_ = TensorVar('self')
    osz = Array('output_size.data', IntSort(), IntSort())
    out_ok = Or(
        out.numel() == IntVal(0),
        And(out.ndim == IntVal(4),
            out.size(0) == self_.size(0),
            out.size(1) == self_.size(1),
            out.size(2) == Select(osz, IntVal(0)),
            out.size(3) == Select(osz, IntVal(1))),
    )
    return [
        Not(And(out_ok, Or((IntVal(0) == Int('self.dtype')), (IntVal(5) == Int('self.dtype')), (IntVal(6) == Int('self.dtype')), (IntVal(7) == Int('self.dtype')), (IntVal(15) == Int('self.dtype'))), (Int('self.ndim') == IntVal(4)), (Int('output_size.length') == IntVal(2)), (IntVal(1) <= Select(Array('self.sizes', IntSort(), IntSort()), IntVal(2))), (IntVal(1) <= Select(Array('self.sizes', IntSort(), IntSort()), IntVal(3))), (IntVal(1) <= Select(Array('output_size.data', IntSort(), IntSort()), IntVal(0))), (IntVal(1) <= Select(Array('output_size.data', IntSort(), IntSort()), IntVal(1))), (Int('out.dtype') == Int('self.dtype')), out_ok)),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    self = TensorVar('self')
    output_size = IntArrayVar('output_size')
    out = TensorVar('out')
    return {'self': self, 'output_size': output_size, 'out': out}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
