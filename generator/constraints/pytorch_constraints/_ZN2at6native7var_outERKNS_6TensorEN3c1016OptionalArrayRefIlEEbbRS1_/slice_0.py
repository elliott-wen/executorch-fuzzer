"""Combined + Z3-optimized constraints for _ZN2at6native7var_outERKNS_6TensorEN3c1016OptionalArrayRefIlEEbbRS1_.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

from z3 import *
from mobile.generator.constraints.model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar

ENTRY = '_ZN2at6native7var_outERKNS_6TensorEN3c1016OptionalArrayRefIlEEbbRS1_ (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('self', 'Tensor'), ('dim', 'int'), ('unbiased', 'bool'), ('keepdim', 'bool'), ('out', 'Tensor')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    _self = TensorVar('self'); _out = TensorVar('out')
    _out_ok = _std_out_shape_ok(_self, _out, Int('dim'), Bool('keepdim'))
    return [
        Not(And(Or((Int('self.dtype') == IntVal(5)), (Int('self.dtype') == IntVal(6)), (Int('self.dtype') == IntVal(7)), (Int('self.dtype') == IntVal(15))), Or((Int('out.dtype') == IntVal(5)), (Int('out.dtype') == IntVal(6)), (Int('out.dtype') == IntVal(7)), (Int('out.dtype') == IntVal(15))), Or(And((Int('self.ndim') == IntVal(0)), Or((Int('dim') == IntVal(0)), (Int('dim') == IntVal(-1)))), And(Not((Int('self.ndim') <= IntVal(0))), (Int('dim') >= (IntVal(-1) * Int('self.ndim'))), Not((Int('self.ndim') <= Int('dim'))))), _out_ok)),
    ]


def _std_out_shape_ok(_self, _out, _d, _keep):
    _MAXD = 8
    _nd = _self.ndim
    _dw = If(_d < IntVal(0), _d + _nd, _d)
    _keep_ok = And(_out.ndim == _nd, *[
        Implies(IntVal(i) < _nd,
                Select(_out.sizes, IntVal(i)) ==
                If(IntVal(i) == _dw, IntVal(1), Select(_self.sizes, IntVal(i))))
        for i in range(_MAXD)])
    _nd_nk = If(_nd <= IntVal(0), IntVal(0), _nd - IntVal(1))
    _nokeep_ok = And(_out.ndim == _nd_nk, *[
        Implies(IntVal(i) < _nd_nk,
                Select(_out.sizes, IntVal(i)) ==
                If(IntVal(i) < _dw, Select(_self.sizes, IntVal(i)),
                   Select(_self.sizes, IntVal(i) + IntVal(1))))
        for i in range(_MAXD)])
    return Or(_out.numel() == 0, If(_keep, _keep_ok, _nokeep_ok))


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    pass
    return {}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
