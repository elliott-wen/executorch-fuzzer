"""Combined + Z3-optimized constraints for _ZN2at6native35_padded_dense_to_jagged_forward_cpuERKNS_6TensorEN3c108ArrayRefIS1_EESt8optionalIlE.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

from z3 import *
from mobile.generator.constraints.model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar

ENTRY = '_ZN2at6native35_padded_dense_to_jagged_forward_cpuERKNS_6TensorEN3c108ArrayRefIS1_EESt8optionalIlE (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('dense', 'Tensor'), ('offsets', 'Tensor[]'), ('total_L', 'int?')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    return [
        Not(And((IntVal(1) == Int('offsets.length')), (IntVal(1) == Int('offsets[0].ndim')), Or((Int('offsets[0].dtype') == IntVal(0)), (Int('offsets[0].dtype') == IntVal(1)), (Int('offsets[0].dtype') == IntVal(2)), (Int('offsets[0].dtype') == IntVal(3)), (Int('offsets[0].dtype') == IntVal(4)), (Int('offsets[0].dtype') == IntVal(27)), (Int('offsets[0].dtype') == IntVal(28)), (Int('offsets[0].dtype') == IntVal(29))), (IntVal(1) == Select(Array('offsets[0].sizes', IntSort(), IntSort()), IntVal(0))), (IntVal(2) <= Int('dense.ndim')), Not(Bool('total_L.present')))),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    dense = TensorVar('dense')
    offsets = TensorListVar('offsets')
    return {'dense': dense, 'offsets': offsets}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
