"""Combined + Z3-optimized constraints for _ZN2at6native11unfold_copyERKNS_6TensorElll.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

from z3 import *
from mobile.generator.constraints.model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar

ENTRY = '_ZN2at6native11unfold_copyERKNS_6TensorElll (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('self', 'Tensor'), ('dimension', 'int'), ('size', 'int'), ('step', 'int')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    return [
        Not(And(((Int('dimension') + If((Int('self.ndim') == IntVal(0)), IntVal(1), Int('self.ndim'))) >= IntVal(0)), Not((If((Int('self.ndim') == IntVal(0)), IntVal(1), Int('self.ndim')) <= If((Int('dimension') >= IntVal(0)), Int('dimension'), (Int('dimension') + If((Int('self.ndim') == IntVal(0)), IntVal(1), Int('self.ndim')))))), (Int('size') >= IntVal(0)), (Int('size') <= If((Int('self.ndim') == IntVal(0)), IntVal(1), Select(Array('self.sizes', IntSort(), IntSort()), If((Int('dimension') >= IntVal(0)), Int('dimension'), (Int('dimension') + If((Int('self.ndim') == IntVal(0)), IntVal(1), Int('self.ndim'))))))), Not((Int('step') <= IntVal(0))))),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    pass
    return {}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
