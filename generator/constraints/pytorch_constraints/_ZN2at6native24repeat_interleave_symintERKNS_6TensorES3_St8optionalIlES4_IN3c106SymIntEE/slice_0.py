"""Combined + Z3-optimized constraints for _ZN2at6native24repeat_interleave_symintERKNS_6TensorES3_St8optionalIlES4_IN3c106SymIntEE.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

from z3 import *
from mobile.generator.constraints.model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar

ENTRY = '_ZN2at6native24repeat_interleave_symintERKNS_6TensorES3_St8optionalIlES4_IN3c106SymIntEE (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('self', 'Tensor'), ('repeats', 'Tensor'), ('dim', 'int?'), ('output_size', 'int?')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    return [
        Not(And(Or((Int('repeats.dtype') == IntVal(3)), (Int('repeats.dtype') == IntVal(4))), (Int('repeats.ndim') <= IntVal(1)), (Real('repeats.data_lo') >= RealVal(0)), Or(Not(Bool('dim.present')), And((Int('dim.val') >= (IntVal(-1) * Int('self.ndim'))), Not((Int('self.ndim') <= Int('dim.val'))))), Or((Int('repeats.ndim') == IntVal(0)), (Select(Array('repeats.sizes', IntSort(), IntSort()), IntVal(0)) == IntVal(1)), And(Bool('dim.present'), (Int('dim.val') >= (IntVal(-1) * Int('self.ndim'))), Not((Int('self.ndim') <= Int('dim.val'))), (Select(Array('repeats.sizes', IntSort(), IntSort()), IntVal(0)) == Select(Array('self.sizes', IntSort(), IntSort()), If((If((Int('dim.val') >= IntVal(0)), Int('dim.val'), (Int('dim.val') + Int('self.ndim'))) >= IntVal(0)), If((Int('dim.val') >= IntVal(0)), Int('dim.val'), (Int('dim.val') + Int('self.ndim'))), (Int('self.ndim') + If((Int('dim.val') >= IntVal(0)), Int('dim.val'), (Int('dim.val') + Int('self.ndim'))))))))), Not(Bool('output_size.present')))),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    pass
    return {}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
