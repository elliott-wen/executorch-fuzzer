"""Combined + Z3-optimized constraints for _ZN2at6native19quantize_per_tensorERKNS_6TensorEdlN3c1010ScalarTypeE.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

from z3 import *
from mobile.generator.constraints.model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar

ENTRY = '_ZN2at6native19quantize_per_tensorERKNS_6TensorEdlN3c1010ScalarTypeE (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('self', 'Tensor'), ('scale', 'float'), ('zero_point', 'int'), ('dtype', 'int')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    return [
        Not(And((Int('self.dtype') == IntVal(6)), Or((Int('dtype') == IntVal(12)), (Int('dtype') == IntVal(13)), (Int('dtype') == IntVal(14))), Or(And((Int('dtype') == IntVal(12)), (Int('zero_point') >= IntVal(-128)), (Int('zero_point') <= IntVal(127))), And((Int('dtype') == IntVal(13)), (Int('zero_point') >= IntVal(0)), (Int('zero_point') <= IntVal(255))), And((Int('dtype') == IntVal(14)), (Int('zero_point') >= IntVal(-2147483648)), (Int('zero_point') <= IntVal(2147483647)))))),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    pass
    return {}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
