"""Combined + Z3-optimized constraints for _ZN2at6native10normal_outERKNS_6TensorEddSt8optionalINS_9GeneratorEERS1_.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

from z3 import *
from mobile.generator.constraints.model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar
from mobile.generator.constraints.model import same_shape, out_is_broadcast

ENTRY = '_ZN2at6native10normal_outERKNS_6TensorEddSt8optionalINS_9GeneratorEERS1_ (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('self', 'Tensor'), ('mean', 'float'), ('std', 'float'), ('generator', 'Generator?'), ('out', 'Tensor')]


def get_params() -> list:
    return _PARAMS


def _get_constraints_orig() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    return [
        Not(And((Real('std') >= RealVal(0)), Or((Int('self.dtype') == IntVal(5)), (Int('self.dtype') == IntVal(15)), (Int('self.dtype') == IntVal(6)), (Int('self.dtype') == IntVal(7)), (Int('self.dtype') == IntVal(8)), (Int('self.dtype') == IntVal(9)), (Int('self.dtype') == IntVal(10))), (Int('out.dtype') == Int('self.dtype')))),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    self = TensorVar('self')
    mean = Real('mean')
    std = Real('std')
    out = TensorVar('out')
    return {'self': self, 'mean': mean, 'std': std, 'out': out}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s


def get_constraints() -> list:
    # OUTPUT-SHAPE FIX: normal(mean_tensor, mean, std) -> out.shape == mean-tensor (self) shape.
    # resize_output hard-errors on wrong non-empty out; require empty or exact.
    self = TensorVar('self')
    out = TensorVar('out')
    out_ok = Or(out.numel() == 0, same_shape(out, self))
    return [Or(c, Not(out_ok)) for c in _get_constraints_orig()]
