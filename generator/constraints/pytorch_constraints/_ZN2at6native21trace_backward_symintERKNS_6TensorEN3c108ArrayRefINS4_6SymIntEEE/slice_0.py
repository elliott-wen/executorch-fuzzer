"""Combined + Z3-optimized constraints for _ZN2at6native21trace_backward_symintERKNS_6TensorEN3c108ArrayRefINS4_6SymIntEEE.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

from z3 import *
from mobile.generator.constraints.model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar

ENTRY = '_ZN2at6native21trace_backward_symintERKNS_6TensorEN3c108ArrayRefINS4_6SymIntEEE (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('grad', 'Tensor'), ('sizes', 'int[]')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    return [
        Not(And((Int('sizes.length') == IntVal(2)), (Select(Array('sizes.data', IntSort(), IntSort()), IntVal(0)) >= IntVal(0)), (Select(Array('sizes.data', IntSort(), IntSort()), IntVal(1)) >= IntVal(0)), (Int('grad.ndim') == IntVal(0)), Or((Int('grad.dtype') == IntVal(0)), (Int('grad.dtype') == IntVal(1)), (Int('grad.dtype') == IntVal(2)), (Int('grad.dtype') == IntVal(3)), (Int('grad.dtype') == IntVal(4)), (Int('grad.dtype') == IntVal(5)), (Int('grad.dtype') == IntVal(6)), (Int('grad.dtype') == IntVal(7)), (Int('grad.dtype') == IntVal(8)), (Int('grad.dtype') == IntVal(9)), (Int('grad.dtype') == IntVal(10)), (Int('grad.dtype') == IntVal(11)), (Int('grad.dtype') == IntVal(15))))),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    pass
    return {}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
