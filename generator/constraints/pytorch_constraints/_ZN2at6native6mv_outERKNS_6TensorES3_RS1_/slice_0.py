"""Combined + Z3-optimized constraints for _ZN2at6native6mv_outERKNS_6TensorES3_RS1_.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

from z3 import *
from mobile.generator.constraints.model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar
from mobile.generator.constraints.model import same_shape, out_is_broadcast

ENTRY = '_ZN2at6native6mv_outERKNS_6TensorES3_RS1_ (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('self', 'Tensor'), ('vec', 'Tensor'), ('out', 'Tensor')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    self = TensorVar('self')
    out = TensorVar('out')
    # mv: self=[n,m], vec=[m] -> out=[n]
    out_ok = Or(out.numel() == 0,
                And(out.ndim == 1, out.size(0) == self.size(0)))
    return [
        Not(And((IntVal(2) == Int('self.ndim')), (IntVal(1) == Int('vec.ndim')), (Select(Array('self.sizes', IntSort(), IntSort()), IntVal(1)) == Select(Array('vec.sizes', IntSort(), IntSort()), IntVal(0))), (Int('self.dtype') == Int('vec.dtype')), Or((IntVal(0) == Int('self.dtype')), (IntVal(1) == Int('self.dtype')), (IntVal(2) == Int('self.dtype')), (IntVal(3) == Int('self.dtype')), (IntVal(4) == Int('self.dtype')), (IntVal(5) == Int('self.dtype')), (IntVal(6) == Int('self.dtype')), (IntVal(7) == Int('self.dtype')), (IntVal(9) == Int('self.dtype')), (IntVal(10) == Int('self.dtype')), (IntVal(15) == Int('self.dtype'))), (Int('out.dtype') == Int('self.dtype')), out_ok)),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    self = TensorVar('self')
    vec = TensorVar('vec')
    out = TensorVar('out')
    return {'self': self, 'vec': vec, 'out': out}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
