"""Combined + Z3-optimized constraints for _ZN2at6native12logspace_outERKNS_6TensorES3_ldRS1_.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

from z3 import *
from mobile.generator.constraints.model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar

ENTRY = '_ZN2at6native12logspace_outERKNS_6TensorES3_ldRS1_ (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('start', 'Tensor'), ('end', 'Tensor'), ('steps', 'int'), ('base', 'float'), ('out', 'Tensor')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    # logspace fills out[i] = base^x for exponents x interpolated from start to
    # end (RangeFactories.cpp std::pow(scalar_base, ...)).  Random large exponents
    # or base make base^x overflow the output dtype ("value cannot be converted to
    # type X without overflow").  Clamp the exponent tensor data (start/end are
    # 0-dim tensors whose .item() is the exponent) and base into a tame positive
    # window so base^x stays small (<= 4^3 = 64, safe even for int8).
    start = TensorVar('start')
    end = TensorVar('end')
    base = Real('base')
    return [
        Not(And(Not((IntVal(11) == Int('out.dtype'))), Not((IntVal(8) == Int('out.dtype'))), Not((IntVal(23) == Int('out.dtype'))), Not((IntVal(24) == Int('out.dtype'))), Not((IntVal(25) == Int('out.dtype'))), Not((IntVal(26) == Int('out.dtype'))), Not((IntVal(44) == Int('out.dtype'))), Not((IntVal(27) == Int('out.dtype'))), Not((IntVal(28) == Int('out.dtype'))), Not((IntVal(29) == Int('out.dtype'))), (Int('start.ndim') == IntVal(0)), (Int('end.ndim') == IntVal(0)), (IntVal(0) <= Int('steps')),
            start.data_lo >= 0, start.data_hi <= 3,
            end.data_lo >= 0, end.data_hi <= 3,
            base >= 0, base <= 4)),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    start = TensorVar('start')
    end = TensorVar('end')
    out = TensorVar('out')
    return {'start': start, 'end': end, 'out': out}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
