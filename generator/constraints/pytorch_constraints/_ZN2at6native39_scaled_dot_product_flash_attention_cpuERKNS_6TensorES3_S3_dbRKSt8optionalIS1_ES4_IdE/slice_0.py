"""Combined + Z3-optimized constraints for _ZN2at6native39_scaled_dot_product_flash_attention_cpuERKNS_6TensorES3_S3_dbRKSt8optionalIS1_ES4_IdE.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

from z3 import *
from mobile.generator.constraints.model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar

ENTRY = '_ZN2at6native39_scaled_dot_product_flash_attention_cpuERKNS_6TensorES3_S3_dbRKSt8optionalIS1_ES4_IdE (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('query', 'Tensor'), ('key', 'Tensor'), ('value', 'Tensor'), ('dropout_p', 'float'), ('is_causal', 'bool'), ('attn_mask', 'Tensor?'), ('scale', 'float?')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    return [
        Not(And(Or((Int('query.dtype') == IntVal(5)), (Int('query.dtype') == IntVal(6)), (Int('query.dtype') == IntVal(7)), (Int('query.dtype') == IntVal(15))), (Int('query.dtype') == Int('key.dtype')), (Int('query.dtype') == Int('value.dtype')), (IntVal(4) == Int('query.ndim')), (IntVal(4) == Int('key.ndim')), (IntVal(4) == Int('value.ndim')), (RealVal(0) == Real('dropout_p')), (Select(Array('query.sizes', IntSort(), IntSort()), IntVal(3)) == Select(Array('value.sizes', IntSort(), IntSort()), IntVal(3))), (Select(Array('key.sizes', IntSort(), IntSort()), IntVal(3)) == Select(Array('value.sizes', IntSort(), IntSort()), IntVal(3))), (Select(Array('key.sizes', IntSort(), IntSort()), IntVal(1)) <= Select(Array('query.sizes', IntSort(), IntSort()), IntVal(1))), Not(Bool('attn_mask.present')))),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    query = TensorVar('query')
    key = TensorVar('key')
    value = TensorVar('value')
    attn_mask = OptVar('attn_mask', TensorVar('attn_mask' + '.val'))
    return {'query': query, 'key': key, 'value': value, 'attn_mask': attn_mask}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
