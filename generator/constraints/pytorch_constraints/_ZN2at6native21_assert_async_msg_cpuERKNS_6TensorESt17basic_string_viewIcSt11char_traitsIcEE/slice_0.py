"""Combined + Z3-optimized constraints for _ZN2at6native21_assert_async_msg_cpuERKNS_6TensorESt17basic_string_viewIcSt11char_traitsIcEE.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

from z3 import *
from mobile.generator.constraints.model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar

ENTRY = '_ZN2at6native21_assert_async_msg_cpuERKNS_6TensorESt17basic_string_viewIcSt11char_traitsIcEE (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('self', 'Tensor'), ('assert_msg', 'str')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    return [
        Not(And(Or((Int('self.ndim') <= IntVal(0)), (Select(Array('self.sizes', IntSort(), IntSort()), IntVal(0)) == IntVal(1))), Or((Int('self.ndim') <= IntVal(1)), (Select(Array('self.sizes', IntSort(), IntSort()), IntVal(1)) == IntVal(1))), Or((Int('self.ndim') <= IntVal(2)), (Select(Array('self.sizes', IntSort(), IntSort()), IntVal(2)) == IntVal(1))), Or((Int('self.ndim') <= IntVal(3)), (Select(Array('self.sizes', IntSort(), IntSort()), IntVal(3)) == IntVal(1))), Or((Int('self.ndim') <= IntVal(4)), (Select(Array('self.sizes', IntSort(), IntSort()), IntVal(4)) == IntVal(1))), And(
            # Float8 dtypes are excluded: the concretizer cannot clamp Float8
            # data away from zero (clamp_ is unsupported for Float8 and the
            # randomizer leaves it at 0), so a Float8 single-element tensor here
            # always concretizes to 0 and fires the is_nonzero assert.  PyTorch
            # itself accepts nonzero Float8, but we cannot materialize that data.
            And((Int('self.dtype') != IntVal(23)), (Int('self.dtype') != IntVal(24)), (Int('self.dtype') != IntVal(25)), (Int('self.dtype') != IntVal(26)), (Int('self.dtype') != IntVal(44))),
            Or(And((Real('self.data_lo') >= RealVal(1)), (Real('self.data_hi') >= RealVal(1))), And((Real('self.data_hi') <= RealVal(-1)), (Real('self.data_lo') <= RealVal(-1)), Or((Int('self.dtype') == IntVal(1)), (Int('self.dtype') == IntVal(2)), (Int('self.dtype') == IntVal(3)), (Int('self.dtype') == IntVal(4)), (Int('self.dtype') == IntVal(5)), (Int('self.dtype') == IntVal(15)), (Int('self.dtype') == IntVal(6)), (Int('self.dtype') == IntVal(7)), (Int('self.dtype') == IntVal(8)), (Int('self.dtype') == IntVal(9)), (Int('self.dtype') == IntVal(10)))))))),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    self = TensorVar('self')
    return {'self': self}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
