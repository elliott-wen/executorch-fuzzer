"""Combined + Z3-optimized constraints for _ZN2at6native38structured_adaptive_max_pool2d_out_cpu4implERKNS_6TensorEN3c108ArrayRefIlEES4_S4_.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

from z3 import *
from mobile.generator.constraints.model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar, MAX_DIM


def _pool_out_ok(out, self_t, oH, oW):
    """out is empty OR out == self leading dims [:-2] ++ [oH, oW]."""
    nb = self_t.ndim - IntVal(2)
    conds = [out.ndim == self_t.ndim]
    for i in range(MAX_DIM):
        conds.append(Implies(IntVal(i) < nb, out.size(i) == self_t.size(i)))
    conds.append(out.size(self_t.ndim - IntVal(2)) == oH)
    conds.append(out.size(self_t.ndim - IntVal(1)) == oW)
    return Or(out.numel() == 0, And(*conds))

ENTRY = '_ZN2at6native38structured_adaptive_max_pool2d_out_cpu4implERKNS_6TensorEN3c108ArrayRefIlEES4_S4_ (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('self', 'Tensor'), ('output_size', 'int[]'), ('out', 'Tensor'), ('indices', 'Tensor')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    self_t = TensorVar('self')
    out = TensorVar('out')
    indices = TensorVar('indices')
    osz = Array('output_size.data', IntSort(), IntSort())
    oH = Select(osz, IntVal(0))
    oW = Select(osz, IntVal(1))
    out_ok_out = _pool_out_ok(out, self_t, oH, oW)
    out_ok_idx = _pool_out_ok(indices, self_t, oH, oW)
    return [
        Not(And(out_ok_out, out_ok_idx, Or((Int('self.dtype') == IntVal(6)), (Int('self.dtype') == IntVal(7)), (Int('self.dtype') == IntVal(5)), (Int('self.dtype') == IntVal(15))), (Int('out.dtype') == Int('self.dtype')), (Int('indices.dtype') == IntVal(4)), Or((Int('self.ndim') == IntVal(3)), (Int('self.ndim') == IntVal(4))), Or(And((Int('self.ndim') == IntVal(3)), Not((Select(Array('self.sizes', IntSort(), IntSort()), IntVal(0)) <= IntVal(0))), Not((Select(Array('self.sizes', IntSort(), IntSort()), IntVal(1)) <= IntVal(0))), Not((Select(Array('self.sizes', IntSort(), IntSort()), IntVal(2)) <= IntVal(0)))), And((Int('self.ndim') == IntVal(4)), Not((Select(Array('self.sizes', IntSort(), IntSort()), IntVal(1)) <= IntVal(0))), Not((Select(Array('self.sizes', IntSort(), IntSort()), IntVal(2)) <= IntVal(0))), Not((Select(Array('self.sizes', IntSort(), IntSort()), IntVal(3)) <= IntVal(0))))), (Int('output_size.length') == IntVal(2)), (Select(Array('output_size.data', IntSort(), IntSort()), IntVal(0)) >= IntVal(0)), (Select(Array('output_size.data', IntSort(), IntSort()), IntVal(1)) >= IntVal(0)))),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    self = TensorVar('self')
    output_size = IntArrayVar('output_size')
    out = TensorVar('out')
    indices = TensorVar('indices')
    return {'self': self, 'output_size': output_size, 'out': out, 'indices': indices}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
