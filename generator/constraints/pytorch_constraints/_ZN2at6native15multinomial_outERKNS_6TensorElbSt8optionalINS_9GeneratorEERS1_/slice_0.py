"""Combined + Z3-optimized constraints for _ZN2at6native15multinomial_outERKNS_6TensorElbSt8optionalINS_9GeneratorEERS1_.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

from z3 import *
from mobile.generator.constraints.model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar

ENTRY = '_ZN2at6native15multinomial_outERKNS_6TensorElbSt8optionalINS_9GeneratorEERS1_ (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('self', 'Tensor'), ('num_samples', 'int'), ('replacement', 'bool'), ('generator', 'Generator?'), ('out', 'Tensor')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    return [
        Not(And(Or((Int('self.ndim') == IntVal(1)), (Int('self.ndim') == IntVal(2))), Or((IntVal(5) == Int('self.dtype')), (IntVal(6) == Int('self.dtype')), (IntVal(7) == Int('self.dtype')), (IntVal(15) == Int('self.dtype'))), Not((Real('self.data_lo') <= RealVal(0))), (Real('self.data_hi') <= RealVal(1)), (Real('self.data_hi') >= RealVal(1)), (IntVal(4) == Int('out.dtype')), (Select(Array('self.sizes', IntSort(), IntSort()), (IntVal(-1) + Int('self.ndim'))) >= IntVal(1)), (Int('num_samples') >= IntVal(1)), Or(Bool('replacement'), (Int('num_samples') <= Select(Array('self.sizes', IntSort(), IntSort()), (IntVal(-1) + Int('self.ndim'))))))),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    self = TensorVar('self')
    num_samples = Int('num_samples')
    replacement = Bool('replacement')
    out = TensorVar('out')
    return {'self': self, 'num_samples': num_samples, 'replacement': replacement, 'out': out}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
