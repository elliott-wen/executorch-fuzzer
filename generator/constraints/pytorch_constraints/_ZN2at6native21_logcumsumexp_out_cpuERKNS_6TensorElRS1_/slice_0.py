"""Combined + Z3-optimized constraints for _ZN2at6native21_logcumsumexp_out_cpuERKNS_6TensorElRS1_.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

from z3 import *
from mobile.generator.constraints.model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar
from mobile.generator.constraints.model import same_shape, out_is_broadcast, expandable_to, broadcastable

ENTRY = '_ZN2at6native21_logcumsumexp_out_cpuERKNS_6TensorElRS1_ (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('self', 'Tensor'), ('dim', 'int'), ('out', 'Tensor')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    return [
        Not(And(Or((Int('self.dtype') == IntVal(5)), (Int('self.dtype') == IntVal(6)), (Int('self.dtype') == IntVal(7)), (Int('self.dtype') == IntVal(15)), (Int('self.dtype') == IntVal(9)), (Int('self.dtype') == IntVal(10))), Or(And((Int('self.ndim') == IntVal(0)), Or((Int('dim') == IntVal(0)), (Int('dim') == IntVal(-1)))), And((Int('self.ndim') >= IntVal(1)), (Int('dim') >= (IntVal(-1) * Int('self.ndim'))), Not((Int('self.ndim') <= Int('dim'))))))),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    pass
    return {}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s


# === OUTPUT-SHAPE PATCH ===
_orig_get_constraints = get_constraints


def get_constraints() -> list:
    cs = _orig_get_constraints()
    o=TensorVar('out'); s=TensorVar('self')
    _out_oks=[Or(o.numel()==0, same_shape(o,s))]
    out = []
    for c in cs:
        if c.decl().name() == 'not' and c.num_args() == 1 and c.arg(0).decl().name() == 'and':
            inner = list(c.arg(0).children())
            out.append(Not(And(*inner, *_out_oks)))
        else:
            out.append(c)
    return out
