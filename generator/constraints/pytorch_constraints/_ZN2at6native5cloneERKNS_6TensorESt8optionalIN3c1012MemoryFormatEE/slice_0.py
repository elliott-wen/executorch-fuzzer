"""Combined + Z3-optimized constraints for _ZN2at6native5cloneERKNS_6TensorESt8optionalIN3c1012MemoryFormatEE.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

from z3 import *
from mobile.generator.constraints.model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar

ENTRY = '_ZN2at6native5cloneERKNS_6TensorESt8optionalIN3c1012MemoryFormatEE (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('self', 'Tensor'), ('memory_format', 'int?')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    return [
        Not(And((Int('self.ndim') >= IntVal(0)), Or(Not(Bool('memory_format.present')), And(Or(Not((IntVal(2) == Int('memory_format.val'))), (Int('self.ndim') == IntVal(4))), Or(Not((IntVal(3) == Int('memory_format.val'))), (Int('self.ndim') == IntVal(5))))))),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    self = TensorVar('self')
    memory_format = OptVar('memory_format', Int('memory_format' + '.val'))
    return {'self': self, 'memory_format': memory_format}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
