"""Combined + Z3-optimized constraints for _ZN2at6native38fractional_max_pool3d_backward_out_cpuERKNS_6TensorES3_N3c108ArrayRefIlEES6_S3_RS1_.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

import sys
sys.path.insert(0, '/data/jwen929/pytorch/z3')
from z3 import *
from model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar

ENTRY = '_ZN2at6native38fractional_max_pool3d_backward_out_cpuERKNS_6TensorES3_N3c108ArrayRefIlEES6_S3_RS1_ (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('grad_output', 'Tensor'), ('self', 'Tensor'), ('kernel_size', 'int[]'), ('output_size', 'int[]'), ('indices', 'Tensor'), ('grad_input', 'Tensor')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    return [
        Not(And(Or((Int('self.ndim') == IntVal(4)), (Int('self.ndim') == IntVal(5))), Or((Int('self.dtype') == IntVal(5)), (Int('self.dtype') == IntVal(6)), (Int('self.dtype') == IntVal(7)), (Int('self.dtype') == IntVal(15))), (Int('output_size.length') == IntVal(3)), (Int('grad_output.dtype') == Int('self.dtype')), (Int('grad_input.dtype') == Int('self.dtype')), (Int('indices.dtype') == IntVal(4)), (Int('grad_output.ndim') == Int('self.ndim')), (Select(Array('grad_output.sizes', IntSort(), IntSort()), If((Int('self.ndim') == IntVal(5)), IntVal(2), IntVal(1))) == Select(Array('output_size.data', IntSort(), IntSort()), IntVal(0))), (Select(Array('grad_output.sizes', IntSort(), IntSort()), If((Int('self.ndim') == IntVal(5)), IntVal(3), IntVal(2))) == Select(Array('output_size.data', IntSort(), IntSort()), IntVal(1))), (Select(Array('grad_output.sizes', IntSort(), IntSort()), If((Int('self.ndim') == IntVal(5)), IntVal(4), IntVal(3))) == Select(Array('output_size.data', IntSort(), IntSort()), IntVal(2))), (Int('indices.ndim') == Int('grad_output.ndim')), Or((Int('indices.ndim') <= IntVal(0)), (Select(Array('indices.sizes', IntSort(), IntSort()), IntVal(0)) == Select(Array('grad_output.sizes', IntSort(), IntSort()), IntVal(0)))), Or((Int('indices.ndim') <= IntVal(1)), (Select(Array('indices.sizes', IntSort(), IntSort()), IntVal(1)) == Select(Array('grad_output.sizes', IntSort(), IntSort()), IntVal(1)))), Or((Int('indices.ndim') <= IntVal(2)), (Select(Array('indices.sizes', IntSort(), IntSort()), IntVal(2)) == Select(Array('grad_output.sizes', IntSort(), IntSort()), IntVal(2)))), Or((Int('indices.ndim') <= IntVal(3)), (Select(Array('indices.sizes', IntSort(), IntSort()), IntVal(3)) == Select(Array('grad_output.sizes', IntSort(), IntSort()), IntVal(3)))), Or((Int('indices.ndim') <= IntVal(4)), (Select(Array('indices.sizes', IntSort(), IntSort()), IntVal(4)) == Select(Array('grad_output.sizes', IntSort(), IntSort()), IntVal(4)))), (Real('indices.data_lo') >= RealVal(0)), (Real('indices.data_hi') <= (RealVal(-1) + (ToReal(Select(Array('self.sizes', IntSort(), IntSort()), If((Int('self.ndim') == IntVal(5)), IntVal(2), IntVal(1)))) * ToReal(Select(Array('self.sizes', IntSort(), IntSort()), If((Int('self.ndim') == IntVal(5)), IntVal(3), IntVal(2)))) * ToReal(Select(Array('self.sizes', IntSort(), IntSort()), If((Int('self.ndim') == IntVal(5)), IntVal(4), IntVal(3))))))),
        # plane/batch dims of self (input) must not EXCEED grad_output's: the backward
        # frame iterates numBatch/numPlanes derived from `self` but indexes the
        # `indices` buffer (sized like grad_output) with those counts; if self has
        # MORE planes/batches it reads past the indices buffer -> OOB garbage index
        # -> index>=0 && index<inputT*inputH*inputW assert (FMaxPool3d.cpp:280).
        # self with FEWER planes/batches is fine (loop runs short, stays in bounds),
        # so the precise bound is <=, not == (matches PyTorch: only over-count crashes).
        # ndim4: planeDim=0 -> self.size(0) <= grad_output.size(0).
        # ndim5: numBatch=self.size(0), numPlanes=self.size(1) -> both <= grad_output's.
        (Select(Array('self.sizes', IntSort(), IntSort()), IntVal(0)) <= Select(Array('grad_output.sizes', IntSort(), IntSort()), IntVal(0))),
        Or((Int('self.ndim') != IntVal(5)), (Select(Array('self.sizes', IntSort(), IntSort()), IntVal(1)) <= Select(Array('grad_output.sizes', IntSort(), IntSort()), IntVal(1)))))),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    grad_output = TensorVar('grad_output')
    self = TensorVar('self')
    indices = TensorVar('indices')
    grad_input = TensorVar('grad_input')
    return {'grad_output': grad_output, 'self': self, 'indices': indices, 'grad_input': grad_input}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
