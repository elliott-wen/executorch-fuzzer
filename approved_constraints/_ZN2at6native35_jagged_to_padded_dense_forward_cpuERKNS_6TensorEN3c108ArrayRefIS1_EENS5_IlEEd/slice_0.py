"""Combined + Z3-optimized constraints for _ZN2at6native35_jagged_to_padded_dense_forward_cpuERKNS_6TensorEN3c108ArrayRefIS1_EENS5_IlEEd.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

import sys
sys.path.insert(0, '/data/jwen929/pytorch/z3')
from z3 import *
from model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar

ENTRY = '_ZN2at6native35_jagged_to_padded_dense_forward_cpuERKNS_6TensorEN3c108ArrayRefIS1_EENS5_IlEEd (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('values', 'Tensor'), ('offsets', 'Tensor[]'), ('max_lengths', 'int[]'), ('padding_value', 'float')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    return [
        Not(And((Int('offsets.length') == IntVal(1)), (Int('max_lengths.length') == IntVal(1)), (Select(Array('max_lengths.data', IntSort(), IntSort()), IntVal(0)) >= IntVal(0)), (Int('values.ndim') >= IntVal(1)), Or((IntVal(0) == Int('values.dtype')), (IntVal(1) == Int('values.dtype')), (IntVal(2) == Int('values.dtype')), (IntVal(3) == Int('values.dtype')), (IntVal(4) == Int('values.dtype')), (IntVal(5) == Int('values.dtype')), (IntVal(6) == Int('values.dtype')), (IntVal(7) == Int('values.dtype')), (IntVal(11) == Int('values.dtype')), (IntVal(15) == Int('values.dtype'))), (Int('offsets[0].ndim') == IntVal(1)), (Select(Array('offsets[0].sizes', IntSort(), IntSort()), IntVal(0)) >= IntVal(1)), (Real('offsets[0].data_lo') >= RealVal(0)), (Real('offsets[0].data_hi') <= ToReal(Select(Array('values.sizes', IntSort(), IntSort()), IntVal(0)))))),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    values = TensorVar('values')
    offsets = TensorListVar('offsets')
    return {'values': values, 'offsets': offsets}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
