"""Combined + Z3-optimized constraints for _ZN2at6native17nll_loss2d_symintERKNS_6TensorES3_RKSt8optionalIS1_ElN3c106SymIntE.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

import sys
sys.path.insert(0, '/data/jwen929/pytorch/z3')
from z3 import *
from model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar

ENTRY = '_ZN2at6native17nll_loss2d_symintERKNS_6TensorES3_RKSt8optionalIS1_ElN3c106SymIntE (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('self', 'Tensor'), ('target', 'Tensor'), ('weight', 'Tensor?'), ('reduction', 'int'), ('ignore_index', 'int')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    return [
        Not(And((IntVal(4) == Int('self.ndim')), (IntVal(3) == Int('target.ndim')), Or((IntVal(5) == Int('self.dtype')), (IntVal(15) == Int('self.dtype')), (IntVal(6) == Int('self.dtype')), (IntVal(7) == Int('self.dtype'))), (IntVal(4) == Int('target.dtype')), (Select(Array('self.sizes', IntSort(), IntSort()), IntVal(0)) == Select(Array('target.sizes', IntSort(), IntSort()), IntVal(0))), (Select(Array('self.sizes', IntSort(), IntSort()), IntVal(2)) == Select(Array('target.sizes', IntSort(), IntSort()), IntVal(1))), (Select(Array('self.sizes', IntSort(), IntSort()), IntVal(3)) == Select(Array('target.sizes', IntSort(), IntSort()), IntVal(2))), Or(Not(Bool('weight.present')), (IntVal(1) == Int('weight.val.ndim'))), Or(Not(Bool('weight.present')), (Int('weight.val.dtype') == Int('self.dtype'))), Or(Not(Bool('weight.present')), (Select(Array('weight.val.sizes', IntSort(), IntSort()), IntVal(0)) == Select(Array('self.sizes', IntSort(), IntSort()), IntVal(1)))), (IntVal(1) <= Select(Array('self.sizes', IntSort(), IntSort()), IntVal(1))), (Real('target.data_lo') >= RealVal(0)), (Real('target.data_lo') <= Real('target.data_hi')), (Real('target.data_hi') <= (RealVal(-1) + ToReal(Select(Array('self.sizes', IntSort(), IntSort()), IntVal(1))))))),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    self = TensorVar('self')
    target = TensorVar('target')
    weight = OptVar('weight', TensorVar('weight' + '.val'))
    return {'self': self, 'target': target, 'weight': weight}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
