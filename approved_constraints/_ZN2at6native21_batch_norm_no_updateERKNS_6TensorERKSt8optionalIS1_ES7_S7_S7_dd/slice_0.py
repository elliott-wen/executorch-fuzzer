"""Combined + Z3-optimized constraints for _ZN2at6native21_batch_norm_no_updateERKNS_6TensorERKSt8optionalIS1_ES7_S7_S7_dd.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

import sys
sys.path.insert(0, '/data/jwen929/pytorch/z3')
from z3 import *
from model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar

ENTRY = '_ZN2at6native21_batch_norm_no_updateERKNS_6TensorERKSt8optionalIS1_ES7_S7_S7_dd (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('input', 'Tensor'), ('weight', 'Tensor?'), ('bias', 'Tensor?'), ('running_mean', 'Tensor?'), ('running_var', 'Tensor?'), ('momentum', 'float'), ('eps', 'float')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    return [
        Not(And(Or((Int('input.dtype') == IntVal(5)), (Int('input.dtype') == IntVal(6)), (Int('input.dtype') == IntVal(7)), (Int('input.dtype') == IntVal(15))), (IntVal(2) <= Int('input.ndim')), (Bool('running_mean.present') == Bool('running_var.present')), Or(And(Or(Not(Bool('weight.present')), (Int('weight.val.dtype') == Int('input.dtype'))), Or(Not(Bool('bias.present')), (Int('bias.val.dtype') == Int('input.dtype'))), Or(Not(Bool('running_mean.present')), (Int('running_mean.val.dtype') == Int('input.dtype'))), Or(Not(Bool('running_var.present')), (Int('running_var.val.dtype') == Int('input.dtype')))), And(Or((Int('input.dtype') == IntVal(5)), (Int('input.dtype') == IntVal(15))), Or(Not(Bool('weight.present')), (Int('weight.val.dtype') == IntVal(6))), Or(Not(Bool('bias.present')), (Int('bias.val.dtype') == IntVal(6))), Or(Not(Bool('running_mean.present')), (Int('running_mean.val.dtype') == IntVal(6))), Or(Not(Bool('running_var.present')), (Int('running_var.val.dtype') == IntVal(6))))), Or(Not(Bool('running_mean.present')), (IntVal(1) == Int('running_mean.val.ndim'))), Or(Not(Bool('running_var.present')), (IntVal(1) == Int('running_var.val.ndim'))))),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    pass
    return {}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
