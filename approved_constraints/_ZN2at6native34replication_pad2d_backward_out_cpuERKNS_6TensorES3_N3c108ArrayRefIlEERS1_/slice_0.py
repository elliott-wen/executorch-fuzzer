"""Combined + Z3-optimized constraints for _ZN2at6native34replication_pad2d_backward_out_cpuERKNS_6TensorES3_N3c108ArrayRefIlEERS1_.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

import sys
sys.path.insert(0, '/data/jwen929/pytorch/z3')
from z3 import *
from model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar

ENTRY = '_ZN2at6native34replication_pad2d_backward_out_cpuERKNS_6TensorES3_N3c108ArrayRefIlEERS1_ (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('grad_output', 'Tensor'), ('self', 'Tensor'), ('padding', 'int[]'), ('grad_input', 'Tensor')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    return [
        Not(And((IntVal(4) == Int('padding.length')), Or((IntVal(3) == Int('self.ndim')), (IntVal(4) == Int('self.ndim'))), (Int('grad_output.ndim') == Int('self.ndim')), Or((IntVal(5) == Int('grad_output.dtype')), (IntVal(6) == Int('grad_output.dtype')), (IntVal(7) == Int('grad_output.dtype')), (IntVal(9) == Int('grad_output.dtype')), (IntVal(10) == Int('grad_output.dtype')), (IntVal(15) == Int('grad_output.dtype'))), (Int('self.dtype') == Int('grad_output.dtype')), (Int('grad_input.dtype') == Int('self.dtype')), (Select(Array('grad_output.sizes', IntSort(), IntSort()), If((IntVal(4) == Int('self.ndim')), IntVal(3), IntVal(2))) == (Select(Array('self.sizes', IntSort(), IntSort()), If((IntVal(4) == Int('self.ndim')), IntVal(3), IntVal(2))) + Select(Array('padding.data', IntSort(), IntSort()), IntVal(0)) + Select(Array('padding.data', IntSort(), IntSort()), IntVal(1)))), (Select(Array('grad_output.sizes', IntSort(), IntSort()), If((IntVal(4) == Int('self.ndim')), IntVal(2), IntVal(1))) == (Select(Array('self.sizes', IntSort(), IntSort()), If((IntVal(4) == Int('self.ndim')), IntVal(2), IntVal(1))) + Select(Array('padding.data', IntSort(), IntSort()), IntVal(2)) + Select(Array('padding.data', IntSort(), IntSort()), IntVal(3)))))),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    grad_output = TensorVar('grad_output')
    self = TensorVar('self')
    grad_input = TensorVar('grad_input')
    return {'grad_output': grad_output, 'self': self, 'grad_input': grad_input}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
