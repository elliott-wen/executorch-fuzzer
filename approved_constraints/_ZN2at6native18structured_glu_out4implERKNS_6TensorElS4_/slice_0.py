"""Combined + Z3-optimized constraints for _ZN2at6native18structured_glu_out4implERKNS_6TensorElS4_.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

import sys
sys.path.insert(0, '/data/jwen929/pytorch/z3')
from z3 import *
from model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar
from model import same_shape, out_is_broadcast

ENTRY = '_ZN2at6native18structured_glu_out4implERKNS_6TensorElS4_ (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('self', 'Tensor'), ('dim', 'int'), ('out', 'Tensor')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    _base = [
        Not(And((Int('dim') >= (IntVal(-1) * Int('self.ndim'))), Not((Int('self.ndim') <= Int('dim'))), ((Select(Array('self.sizes', IntSort(), IntSort()), If((Int('dim') >= IntVal(0)), Int('dim'), (Int('self.ndim') + Int('dim')))) % IntVal(2)) == IntVal(0)), Or((Int('self.dtype') == IntVal(5)), (Int('self.dtype') == IntVal(6)), (Int('self.dtype') == IntVal(7)), (Int('self.dtype') == IntVal(15))), Or((Int('out.dtype') == IntVal(5)), (Int('out.dtype') == IntVal(6)), (Int('out.dtype') == IntVal(7)), (Int('out.dtype') == IntVal(15)), (Int('out.dtype') == IntVal(8)), (Int('out.dtype') == IntVal(9)), (Int('out.dtype') == IntVal(10))))),
    ]
    out = TensorVar('out')
    self = TensorVar('self')
    dim = Int('dim')
    from model import MAX_DIM
    w = If(dim < 0, dim + self.ndim, dim)
    _dims = [If(IntVal(i) == w, self.size(i) / 2, self.size(i)) for i in range(MAX_DIM)]
    _shape_ok = And(out.ndim == self.ndim, *[Implies(IntVal(i) < self.ndim, out.size(i) == _dims[i]) for i in range(MAX_DIM)])
    out_ok = Or(out.numel()==0, _shape_ok)
    return [Or(_base[0], Not(out_ok))]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    pass
    return {}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
