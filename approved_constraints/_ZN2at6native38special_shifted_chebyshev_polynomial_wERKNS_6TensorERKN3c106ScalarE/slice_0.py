"""Combined + Z3-optimized constraints for _ZN2at6native38special_shifted_chebyshev_polynomial_wERKNS_6TensorERKN3c106ScalarE.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

import sys
sys.path.insert(0, '/data/jwen929/pytorch/z3')
from z3 import *
from model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar

ENTRY = '_ZN2at6native38special_shifted_chebyshev_polynomial_wERKNS_6TensorERKN3c106ScalarE (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('x', 'Tensor'), ('n', 'Scalar')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    return [
        Not(And(Or((IntVal(6) == Int('x.dtype')), (IntVal(7) == Int('x.dtype')), (IntVal(11) == Int('x.dtype')), (IntVal(0) == Int('x.dtype')), (IntVal(1) == Int('x.dtype')), (IntVal(2) == Int('x.dtype')), (IntVal(3) == Int('x.dtype')), (IntVal(4) == Int('x.dtype')), (IntVal(27) == Int('x.dtype')), (IntVal(28) == Int('x.dtype')), (IntVal(29) == Int('x.dtype'))), Not((Int('n.dtype') == IntVal(8))), Not((Int('n.dtype') == IntVal(9))), Not((Int('n.dtype') == IntVal(10))))),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    pass
    return {}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
