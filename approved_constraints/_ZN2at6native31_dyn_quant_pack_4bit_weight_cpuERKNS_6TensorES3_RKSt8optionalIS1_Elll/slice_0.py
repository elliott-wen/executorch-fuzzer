"""Combined + Z3-optimized constraints for _ZN2at6native31_dyn_quant_pack_4bit_weight_cpuERKNS_6TensorES3_RKSt8optionalIS1_Elll.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

import sys
sys.path.insert(0, '/data/jwen929/pytorch/z3')
from z3 import *
from model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar

ENTRY = '_ZN2at6native31_dyn_quant_pack_4bit_weight_cpuERKNS_6TensorES3_RKSt8optionalIS1_Elll (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('weights', 'Tensor'), ('scales_zeros', 'Tensor'), ('bias', 'Tensor?'), ('block_size', 'int'), ('in_features', 'int'), ('out_features', 'int')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    return [
        Not(And((IntVal(0) == Int('weights.dtype')), Not(And(Not((Int('block_size') == Int('in_features'))), Or(Not((IntVal(0) == (Int('block_size') % IntVal(32)))), Not((IntVal(0) == (Int('in_features') % Int('block_size'))))))))),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    weights = TensorVar('weights')
    scales_zeros = TensorVar('scales_zeros')
    bias = OptVar('bias', TensorVar('bias' + '.val'))
    return {'weights': weights, 'scales_zeros': scales_zeros, 'bias': bias}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
