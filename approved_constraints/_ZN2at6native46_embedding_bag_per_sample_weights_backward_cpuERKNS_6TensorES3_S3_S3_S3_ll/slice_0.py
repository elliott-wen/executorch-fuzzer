"""Combined + Z3-optimized constraints for _ZN2at6native46_embedding_bag_per_sample_weights_backward_cpuERKNS_6TensorES3_S3_S3_S3_ll.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

import sys
sys.path.insert(0, '/data/jwen929/pytorch/z3')
from z3 import *
from model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar

ENTRY = '_ZN2at6native46_embedding_bag_per_sample_weights_backward_cpuERKNS_6TensorES3_S3_S3_S3_ll (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('grad', 'Tensor'), ('weight', 'Tensor'), ('indices', 'Tensor'), ('offsets', 'Tensor'), ('offset2bag', 'Tensor'), ('mode', 'int'), ('padding_idx', 'int')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    return [
        Not(And(Or((IntVal(5) == Int('grad.dtype')), (IntVal(15) == Int('grad.dtype')), (IntVal(6) == Int('grad.dtype')), (IntVal(7) == Int('grad.dtype'))), (IntVal(2) == Int('grad.ndim')), (IntVal(1) <= Select(Array('grad.sizes', IntSort(), IntSort()), IntVal(1))), (IntVal(0) == Int('mode')), (IntVal(2) == Int('weight.ndim')), (Int('weight.dtype') == Int('grad.dtype')), (Select(Array('weight.sizes', IntSort(), IntSort()), IntVal(1)) == Select(Array('grad.sizes', IntSort(), IntSort()), IntVal(1))), (IntVal(1) == Int('indices.ndim')), (IntVal(1) <= Select(Array('indices.sizes', IntSort(), IntSort()), IntVal(0))), Or((IntVal(3) == Int('indices.dtype')), (IntVal(4) == Int('indices.dtype'))), (IntVal(1) == Int('offset2bag.ndim')), (Select(Array('offset2bag.sizes', IntSort(), IntSort()), IntVal(0)) >= Select(Array('indices.sizes', IntSort(), IntSort()), IntVal(0))), (Int('offset2bag.dtype') == Int('indices.dtype')), (IntVal(1) == Int('offsets.ndim')), (Int('offsets.dtype') == Int('indices.dtype')), (Real('indices.data_lo') >= RealVal(0)), (Real('indices.data_hi') <= (RealVal(-1) + ToReal(Select(Array('weight.sizes', IntSort(), IntSort()), IntVal(0))))), (IntVal(1) <= Select(Array('weight.sizes', IntSort(), IntSort()), IntVal(0))), (Real('offset2bag.data_lo') >= RealVal(0)), (Real('offset2bag.data_hi') <= (RealVal(-1) + ToReal(Select(Array('grad.sizes', IntSort(), IntSort()), IntVal(0))))), (IntVal(1) <= Select(Array('grad.sizes', IntSort(), IntSort()), IntVal(0))))),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    grad = TensorVar('grad')
    weight = TensorVar('weight')
    indices = TensorVar('indices')
    offsets = TensorVar('offsets')
    offset2bag = TensorVar('offset2bag')
    return {'grad': grad, 'weight': weight, 'indices': indices, 'offsets': offsets, 'offset2bag': offset2bag}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
