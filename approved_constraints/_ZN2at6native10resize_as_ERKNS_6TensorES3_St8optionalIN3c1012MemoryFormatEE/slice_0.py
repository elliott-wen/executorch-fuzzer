"""Combined + Z3-optimized constraints for _ZN2at6native10resize_as_ERKNS_6TensorES3_St8optionalIN3c1012MemoryFormatEE.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

import sys
sys.path.insert(0, '/data/jwen929/pytorch/z3')
from z3 import *
from model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar

ENTRY = '_ZN2at6native10resize_as_ERKNS_6TensorES3_St8optionalIN3c1012MemoryFormatEE (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('self', 'Tensor'), ('the_template', 'Tensor'), ('memory_format', 'int?')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    return [
        Not(Or(Not(Bool('memory_format.present')), And(Bool('memory_format.present'), Or((IntVal(0) == Int('memory_format.val')), (IntVal(1) == Int('memory_format.val')), And((IntVal(2) == Int('memory_format.val')), (IntVal(4) == Int('the_template.ndim'))), And((IntVal(3) == Int('memory_format.val')), (IntVal(5) == Int('the_template.ndim'))))), And(Not((IntVal(1) == Int('memory_format.val'))), Not((IntVal(2) == Int('memory_format.val'))), Not((IntVal(3) == Int('memory_format.val')))))),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    pass
    return {}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
