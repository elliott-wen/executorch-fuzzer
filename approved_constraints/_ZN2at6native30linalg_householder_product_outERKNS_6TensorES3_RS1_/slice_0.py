"""Combined + Z3-optimized constraints for _ZN2at6native30linalg_householder_product_outERKNS_6TensorES3_RS1_.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

import sys
sys.path.insert(0, '/data/jwen929/pytorch/z3')
from z3 import *
from model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar

ENTRY = '_ZN2at6native30linalg_householder_product_outERKNS_6TensorES3_RS1_ (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('input', 'Tensor'), ('tau', 'Tensor'), ('out', 'Tensor')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    return [
        Not(And(Or((IntVal(6) == Int('input.dtype')), (IntVal(7) == Int('input.dtype')), (IntVal(9) == Int('input.dtype')), (IntVal(10) == Int('input.dtype'))), (Int('tau.dtype') == Int('input.dtype')), (Int('out.dtype') == Int('input.dtype')), (IntVal(2) <= Int('input.ndim')), (Select(Array('input.sizes', IntSort(), IntSort()), (IntVal(-2) + Int('input.ndim'))) >= Select(Array('input.sizes', IntSort(), IntSort()), (IntVal(-1) + Int('input.ndim')))), (Select(Array('input.sizes', IntSort(), IntSort()), (IntVal(-1) + Int('input.ndim'))) >= Select(Array('tau.sizes', IntSort(), IntSort()), (IntVal(-1) + Int('tau.ndim')))), (Int('tau.ndim') == (IntVal(-1) + Int('input.ndim'))), Or((Int('input.ndim') <= IntVal(2)), (Select(Array('input.sizes', IntSort(), IntSort()), IntVal(0)) == Select(Array('tau.sizes', IntSort(), IntSort()), IntVal(0)))), Or((Int('input.ndim') <= IntVal(3)), (Select(Array('input.sizes', IntSort(), IntSort()), IntVal(1)) == Select(Array('tau.sizes', IntSort(), IntSort()), IntVal(1)))), Or((Int('input.ndim') <= IntVal(4)), (Select(Array('input.sizes', IntSort(), IntSort()), IntVal(2)) == Select(Array('tau.sizes', IntSort(), IntSort()), IntVal(2)))))),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    input = TensorVar('input')
    tau = TensorVar('tau')
    out = TensorVar('out')
    return {'input': input, 'tau': tau, 'out': out}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
