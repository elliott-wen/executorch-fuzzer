"""Combined + Z3-optimized constraints for _ZN2at6native53structured__upsample_nearest_exact1d_backward_out_cpu4implERKNS_6TensorEN3c108ArrayRefIlEES7_St8optionalIdES4_.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

import sys
sys.path.insert(0, '/data/jwen929/pytorch/z3')
from z3 import *
from model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar
from model import same_shape, out_is_broadcast

ENTRY = '_ZN2at6native53structured__upsample_nearest_exact1d_backward_out_cpu4implERKNS_6TensorEN3c108ArrayRefIlEES7_St8optionalIdES4_ (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('grad_output', 'Tensor'), ('output_size', 'int[]'), ('input_size', 'int[]'), ('scales', 'float?'), ('grad_input', 'Tensor')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    # grad_input (the out= tensor) must be empty or exactly input_size [N,C,W_in].
    _gi = TensorVar('grad_input')
    _isz = Array('input_size.data', IntSort(), IntSort())
    out_ok = Or(
        _gi.numel() == IntVal(0),
        And(_gi.ndim == IntVal(3),
            _gi.size(0) == Select(_isz, IntVal(0)),
            _gi.size(1) == Select(_isz, IntVal(1)),
            _gi.size(2) == Select(_isz, IntVal(2))),
    )
    return [
        Not(And(out_ok, Or((IntVal(5) == Int('grad_output.dtype')), (IntVal(6) == Int('grad_output.dtype')), (IntVal(7) == Int('grad_output.dtype')), (IntVal(15) == Int('grad_output.dtype'))), (Int('input_size.length') == IntVal(3)), (Int('output_size.length') == IntVal(1)), (IntVal(1) <= Select(Array('input_size.data', IntSort(), IntSort()), IntVal(2))), (IntVal(1) <= Select(Array('output_size.data', IntSort(), IntSort()), IntVal(0))), (Int('grad_output.ndim') == IntVal(3)), (Select(Array('grad_output.sizes', IntSort(), IntSort()), IntVal(0)) == Select(Array('input_size.data', IntSort(), IntSort()), IntVal(0))), (Select(Array('grad_output.sizes', IntSort(), IntSort()), IntVal(1)) == Select(Array('input_size.data', IntSort(), IntSort()), IntVal(1))), (Select(Array('grad_output.sizes', IntSort(), IntSort()), IntVal(2)) == Select(Array('output_size.data', IntSort(), IntSort()), IntVal(0))), (Int('grad_input.dtype') == Int('grad_output.dtype')))),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    grad_output = TensorVar('grad_output')
    output_size = IntArrayVar('output_size')
    input_size = IntArrayVar('input_size')
    grad_input = TensorVar('grad_input')
    return {'grad_output': grad_output, 'output_size': output_size, 'input_size': input_size, 'grad_input': grad_input}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
