"""Combined + Z3-optimized constraints for _ZN2at6native10cummin_outERKNS_6TensorElRS1_S4_.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

import sys
sys.path.insert(0, '/data/jwen929/pytorch/z3')
from z3 import *
from model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar, same_shape

ENTRY = '_ZN2at6native10cummin_outERKNS_6TensorElRS1_S4_ (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('self', 'Tensor'), ('dim', 'int'), ('values', 'Tensor'), ('indices', 'Tensor')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    return [
        Not(And(
            # OUTPUT-SHAPE: both outputs are resized to self.shape (hard check now);
            # each must be empty or EXACTLY same_shape(out, self).
            Or(TensorVar('values').numel() == 0, same_shape(TensorVar('values'), TensorVar('self'))),
            Or(TensorVar('indices').numel() == 0, same_shape(TensorVar('indices'), TensorVar('self'))),
            Or((IntVal(0) == Int('self.dtype')), (IntVal(1) == Int('self.dtype')), (IntVal(2) == Int('self.dtype')), (IntVal(3) == Int('self.dtype')), (IntVal(4) == Int('self.dtype')), (IntVal(5) == Int('self.dtype')), (IntVal(6) == Int('self.dtype')), (IntVal(7) == Int('self.dtype')), (IntVal(11) == Int('self.dtype')), (IntVal(15) == Int('self.dtype'))), (Int('values.dtype') == Int('self.dtype')), (IntVal(4) == Int('indices.dtype')), Or(And((Int('self.ndim') == IntVal(0)), Or((Int('dim') == IntVal(0)), (Int('dim') == IntVal(-1)))), And((Int('self.ndim') >= IntVal(1)), (Int('dim') >= (IntVal(-1) * Int('self.ndim'))), Not((Int('self.ndim') <= Int('dim'))))))),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    pass
    return {}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
