"""Combined + Z3-optimized constraints for _ZN2at6native8mode_outERKNS_6TensorElbRS1_S4_.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

import sys
sys.path.insert(0, '/data/jwen929/pytorch/z3')
from z3 import *
from model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar
from model import same_shape, out_is_broadcast

ENTRY = '_ZN2at6native8mode_outERKNS_6TensorElbRS1_S4_ (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('self', 'Tensor'), ('dim', 'int'), ('keepdim', 'bool'), ('values', 'Tensor'), ('indices', 'Tensor')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    _vv = TensorVar('values'); _iv = TensorVar('indices')
    _out_ok_v = (_vv.numel() == 0)
    _out_ok_i = (_iv.numel() == 0)
    return [
        Not(And((Int('dim') >= (IntVal(-1) * If((Int('self.ndim') <= IntVal(0)), IntVal(1), Int('self.ndim')))), Not((If((Int('self.ndim') <= IntVal(0)), IntVal(1), Int('self.ndim')) <= Int('dim'))), Or(Not((Select(Array('self.sizes', IntSort(), IntSort()), If((If((Int('dim') >= IntVal(0)), Int('dim'), (Int('dim') + Int('self.ndim'))) >= IntVal(0)), If((Int('dim') >= IntVal(0)), Int('dim'), (Int('dim') + Int('self.ndim'))), (Int('self.ndim') + If((Int('dim') >= IntVal(0)), Int('dim'), (Int('dim') + Int('self.ndim')))))) <= IntVal(0))), (Int('self.ndim') == IntVal(0))), (Int('values.dtype') == Int('self.dtype')), (IntVal(4) == Int('indices.dtype')), Not((Int('self.dtype') == IntVal(8))), Not((Int('self.dtype') == IntVal(9))), Not((Int('self.dtype') == IntVal(10))), Not((Int('self.dtype') == IntVal(23))), Not((Int('self.dtype') == IntVal(24))), Not((Int('self.dtype') == IntVal(25))), Not((Int('self.dtype') == IntVal(26))), Not((Int('self.dtype') == IntVal(44))), _out_ok_v, _out_ok_i)),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    pass
    return {}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
