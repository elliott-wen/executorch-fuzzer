"""Combined + Z3-optimized constraints for _ZN2at6native9remainderERKN3c106ScalarERKNS_6TensorE.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

import sys
sys.path.insert(0, '/data/jwen929/pytorch/z3')
from z3 import *
from model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar

ENTRY = '_ZN2at6native9remainderERKN3c106ScalarERKNS_6TensorE (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('self', 'Scalar'), ('other', 'Tensor')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    return [
        Not(And(Not((Int('self.dtype') == IntVal(8))), Not((Int('self.dtype') == IntVal(9))), Not((Int('self.dtype') == IntVal(10))), Or((Int('other.dtype') == IntVal(0)), (Int('other.dtype') == IntVal(1)), (Int('other.dtype') == IntVal(2)), (Int('other.dtype') == IntVal(3)), (Int('other.dtype') == IntVal(4)), (Int('other.dtype') == IntVal(11)), (Int('other.dtype') == IntVal(5)), (Int('other.dtype') == IntVal(15)), (Int('other.dtype') == IntVal(6)), (Int('other.dtype') == IntVal(7))), Not(And((Int('self.dtype') == IntVal(11)), (Int('other.dtype') == IntVal(11)))))),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    self = ScalarVar('self')
    other = TensorVar('other')
    return {'self': self, 'other': other}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
