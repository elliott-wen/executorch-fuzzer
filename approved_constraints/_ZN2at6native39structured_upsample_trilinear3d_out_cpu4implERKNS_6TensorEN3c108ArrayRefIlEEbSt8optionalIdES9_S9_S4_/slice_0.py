"""Combined + Z3-optimized constraints for _ZN2at6native39structured_upsample_trilinear3d_out_cpu4implERKNS_6TensorEN3c108ArrayRefIlEEbSt8optionalIdES9_S9_S4_.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

import sys
sys.path.insert(0, '/data/jwen929/pytorch/z3')
from z3 import *
from model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar

ENTRY = '_ZN2at6native39structured_upsample_trilinear3d_out_cpu4implERKNS_6TensorEN3c108ArrayRefIlEEbSt8optionalIdES9_S9_S4_ (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('self', 'Tensor'), ('output_size', 'int[]'), ('align_corners', 'bool'), ('scales_d', 'float?'), ('scales_h', 'float?'), ('scales_w', 'float?'), ('out', 'Tensor')]


def get_params() -> list:
    return _PARAMS


def _orig_get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    return [
        Not(And(Or((IntVal(5) == Int('self.dtype')), (IntVal(6) == Int('self.dtype')), (IntVal(7) == Int('self.dtype')), (IntVal(15) == Int('self.dtype'))), (Int('self.ndim') == IntVal(5)), (Int('output_size.length') == IntVal(3)), (IntVal(1) <= Select(Array('self.sizes', IntSort(), IntSort()), IntVal(2))), (IntVal(1) <= Select(Array('self.sizes', IntSort(), IntSort()), IntVal(3))), (IntVal(1) <= Select(Array('self.sizes', IntSort(), IntSort()), IntVal(4))), (IntVal(1) <= Select(Array('output_size.data', IntSort(), IntSort()), IntVal(0))), (IntVal(1) <= Select(Array('output_size.data', IntSort(), IntSort()), IntVal(1))), (IntVal(1) <= Select(Array('output_size.data', IntSort(), IntSort()), IntVal(2))), (Int('out.dtype') == Int('self.dtype')))),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    self = TensorVar('self')
    output_size = IntArrayVar('output_size')
    out = TensorVar('out')
    return {'self': self, 'output_size': output_size, 'out': out}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s


from model import TensorVar, IntArrayVar
def _out_shape_ok():
    self_ = TensorVar('self')
    out = TensorVar('out')
    osz = IntArrayVar('output_size')
    exact = And(
        out.ndim == 5,
        out.size(0) == self_.size(0),
        out.size(1) == self_.size(1),
        out.size(2) == Select(osz.data, IntVal(0)),
        out.size(3) == Select(osz.data, IntVal(1)),
        out.size(4) == Select(osz.data, IntVal(2)),
    )
    return Or(out.numel() == 0, exact)


def get_constraints() -> list:
    _base = _orig_get_constraints()
    _e = _base[0]
    _inner = _e.arg(0)
    if _inner.decl().name() == 'and':
        _parts = list(_inner.children())
    else:
        _parts = [_inner]
    return [Not(And(*_parts, _out_shape_ok()))]
