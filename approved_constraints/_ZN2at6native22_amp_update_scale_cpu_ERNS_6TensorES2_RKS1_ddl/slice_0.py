"""Combined + Z3-optimized constraints for _ZN2at6native22_amp_update_scale_cpu_ERNS_6TensorES2_RKS1_ddl.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

import sys
sys.path.insert(0, '/data/jwen929/pytorch/z3')
from z3 import *
from model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar

ENTRY = '_ZN2at6native22_amp_update_scale_cpu_ERNS_6TensorES2_RKS1_ddl (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('self', 'Tensor'), ('growth_tracker', 'Tensor'), ('found_inf', 'Tensor'), ('scale_growth_factor', 'float'), ('scale_backoff_factor', 'float'), ('growth_interval', 'int')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    #
    # NLLLoss/Amp kernel preconditions (AmpGradScalerKernels.cpp
    # _amp_update_scale_cpu_kernel):
    #   current_scale (self) : numel()==1, dtype==Float (6)
    #   growth_tracker        : numel()==1, dtype==Int   (3)
    #   found_inf             : numel()==1, dtype==Float (6)
    # Use the model's numel() (handles full MAX_DIM) instead of a stale
    # ndim<=3 inlined If-chain that ignored dim index 4 (5D tensors).
    self = TensorVar('self')
    growth_tracker = TensorVar('growth_tracker')
    found_inf = TensorVar('found_inf')
    return [
        Not(And(
            growth_tracker.numel() == IntVal(1),
            self.numel() == IntVal(1),
            found_inf.numel() == IntVal(1),
            IntVal(3) == Int('growth_tracker.dtype'),
            IntVal(6) == Int('self.dtype'),
            IntVal(6) == Int('found_inf.dtype'),
        )),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    self = TensorVar('self')
    growth_tracker = TensorVar('growth_tracker')
    found_inf = TensorVar('found_inf')
    return {'self': self, 'growth_tracker': growth_tracker, 'found_inf': found_inf}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
