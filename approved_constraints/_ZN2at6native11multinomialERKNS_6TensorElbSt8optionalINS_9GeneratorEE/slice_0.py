"""Combined + Z3-optimized constraints for _ZN2at6native11multinomialERKNS_6TensorElbSt8optionalINS_9GeneratorEE.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

import sys
sys.path.insert(0, '/data/jwen929/pytorch/z3')
from z3 import *
from model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar

ENTRY = '_ZN2at6native11multinomialERKNS_6TensorElbSt8optionalINS_9GeneratorEE (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('self', 'Tensor'), ('num_samples', 'int'), ('replacement', 'bool'), ('generator', 'Generator?')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    return [
        Not(And(Or((Int('self.ndim') == IntVal(1)), (Int('self.ndim') == IntVal(2))), Or((IntVal(5) == Int('self.dtype')), (IntVal(6) == Int('self.dtype')), (IntVal(7) == Int('self.dtype')), (IntVal(15) == Int('self.dtype'))), (Int('num_samples') >= IntVal(1)), Or(Bool('replacement'), (Int('num_samples') <= Select(Array('self.sizes', IntSort(), IntSort()), (IntVal(-1) + Int('self.ndim'))))), Or(And((Int('self.ndim') == IntVal(2)), (Select(Array('self.sizes', IntSort(), IntSort()), IntVal(0)) == IntVal(0))), And(Not((Real('self.data_lo') <= RealVal(0))), (Select(Array('self.sizes', IntSort(), IntSort()), (IntVal(-1) + Int('self.ndim'))) >= IntVal(1)))))),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    self = TensorVar('self')
    num_samples = Int('num_samples')
    replacement = Bool('replacement')
    return {'self': self, 'num_samples': num_samples, 'replacement': replacement}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
