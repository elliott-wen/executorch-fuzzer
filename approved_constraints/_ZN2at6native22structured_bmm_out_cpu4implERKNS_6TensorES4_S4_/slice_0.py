"""Combined + Z3-optimized constraints for _ZN2at6native22structured_bmm_out_cpu4implERKNS_6TensorES4_S4_.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

import sys
sys.path.insert(0, '/data/jwen929/pytorch/z3')
from z3 import *
from model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar
from model import same_shape, out_is_broadcast

ENTRY = '_ZN2at6native22structured_bmm_out_cpu4implERKNS_6TensorES4_S4_ (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('self', 'Tensor'), ('mat2', 'Tensor'), ('out', 'Tensor')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    self = TensorVar('self')
    mat2 = TensorVar('mat2')
    out = TensorVar('out')
    # bmm: self=[b,n,m], mat2=[b,m,p] -> out=[b,n,p]
    out_ok = Or(out.numel() == 0,
                And(out.ndim == 3,
                    out.size(0) == self.size(0),
                    out.size(1) == self.size(1),
                    out.size(2) == mat2.size(2)))
    return [
        Not(And((Int('out.dtype') == Int('mat2.dtype')), (IntVal(3) == Int('self.ndim')), (IntVal(3) == Int('mat2.ndim')), (Select(Array('mat2.sizes', IntSort(), IntSort()), IntVal(0)) == Select(Array('self.sizes', IntSort(), IntSort()), IntVal(0))), (Int('self.dtype') == Int('mat2.dtype')), Not((IntVal(8) == Int('mat2.dtype'))), Not((IntVal(11) == Int('mat2.dtype'))), Not((IntVal(23) == Int('mat2.dtype'))), Not((IntVal(24) == Int('mat2.dtype'))), Not((IntVal(25) == Int('mat2.dtype'))), Not((IntVal(26) == Int('mat2.dtype'))), Not((IntVal(44) == Int('mat2.dtype'))), Not((IntVal(27) == Int('mat2.dtype'))), Not((IntVal(28) == Int('mat2.dtype'))), Not((IntVal(29) == Int('mat2.dtype'))), (Select(Array('mat2.sizes', IntSort(), IntSort()), IntVal(1)) == Select(Array('self.sizes', IntSort(), IntSort()), IntVal(2))), out_ok)),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    self = TensorVar('self')
    mat2 = TensorVar('mat2')
    out = TensorVar('out')
    return {'self': self, 'mat2': mat2, 'out': out}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
