"""Combined + Z3-optimized constraints for _ZN2at6native39structured_log_softmax_backward_cpu_out4implERKNS_6TensorES4_lN3c1010ScalarTypeES4_.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

import sys
sys.path.insert(0, '/data/jwen929/pytorch/z3')
from z3 import *
from model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar
from model import same_shape, out_is_broadcast

ENTRY = '_ZN2at6native39structured_log_softmax_backward_cpu_out4implERKNS_6TensorES4_lN3c1010ScalarTypeES4_ (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('grad_output', 'Tensor'), ('output', 'Tensor'), ('dim', 'int'), ('input_dtype', 'int'), ('out', 'Tensor')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    _go = TensorVar('grad_output'); _out = TensorVar('out')
    _out_ok = Or(_out.numel() == 0, same_shape(_out, _go))
    return [
        Not(And(_out_ok, Or((Int('grad_output.dtype') == IntVal(5)), (Int('grad_output.dtype') == IntVal(15)), (Int('grad_output.dtype') == IntVal(6)), (Int('grad_output.dtype') == IntVal(7))), (Int('output.dtype') == Int('grad_output.dtype')), (Int('out.dtype') == Int('grad_output.dtype')), Not(And((Int('grad_output.dtype') == IntVal(6)), (Int('input_dtype') == IntVal(5)))), (Int('dim') >= (IntVal(-1) * If((IntVal(0) >= Int('grad_output.ndim')), IntVal(1), Int('grad_output.ndim')))), Not((If((IntVal(0) >= Int('grad_output.ndim')), IntVal(1), Int('grad_output.ndim')) <= Int('dim'))))),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    grad_output = TensorVar('grad_output')
    output = TensorVar('output')
    dim = Int('dim')
    input_dtype = Int('input_dtype')
    out = TensorVar('out')
    return {'grad_output': grad_output, 'output': output, 'dim': dim, 'input_dtype': input_dtype, 'out': out}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
