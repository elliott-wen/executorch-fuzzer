"""Combined + Z3-optimized constraints for _ZN2at6native23_weight_int8pack_mm_cpuERKNS_6TensorES3_S3_.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

import sys
sys.path.insert(0, '/data/jwen929/pytorch/z3')
from z3 import *
from model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar

ENTRY = '_ZN2at6native23_weight_int8pack_mm_cpuERKNS_6TensorES3_S3_ (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('self', 'Tensor'), ('mat2', 'Tensor'), ('scales', 'Tensor')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    return [
        Not(And((IntVal(2) == Int('self.ndim')), Or((IntVal(15) == Int('self.dtype')), (IntVal(5) == Int('self.dtype')), (IntVal(6) == Int('self.dtype'))), (IntVal(2) == Int('mat2.ndim')), (IntVal(1) == Int('mat2.dtype')), (Select(Array('mat2.sizes', IntSort(), IntSort()), IntVal(1)) == Select(Array('self.sizes', IntSort(), IntSort()), IntVal(1))), (IntVal(1) == Int('scales.ndim')), (Select(Array('scales.sizes', IntSort(), IntSort()), IntVal(0)) == Select(Array('mat2.sizes', IntSort(), IntSort()), IntVal(0))), (Int('scales.dtype') == Int('self.dtype')))),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    self = TensorVar('self')
    mat2 = TensorVar('mat2')
    scales = TensorVar('scales')
    return {'self': self, 'mat2': mat2, 'scales': scales}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
