"""Combined + Z3-optimized constraints for _ZN2at6native18masked_softmax_cpuERKNS_6TensorES3_St8optionalIlES5_.

Auto-generated by combine_constraints.py: the per-slice failure paths and axioms
of the source folder, merged into one simplified formula. Consumed by the same
concretizer/test harness as a normal slice (get_axioms carries the whole op
region; the harness still adds its own domain/size axioms)."""

import sys
sys.path.insert(0, '/data/jwen929/pytorch/z3')
from z3 import *
from model import TensorVar, ScalarVar, IntArrayVar, OptVar, TensorListVar

ENTRY = '_ZN2at6native18masked_softmax_cpuERKNS_6TensorES3_St8optionalIlES5_ (combined)'
IN_LOOP = False
LOOP_SAFE = True
_UNRESOLVED = frozenset([])


def get_unresolved_vars() -> frozenset:
    return _UNRESOLVED


_PARAMS = [('self', 'Tensor'), ('mask', 'Tensor'), ('dim', 'int?'), ('mask_type', 'int?')]


def get_params() -> list:
    return _PARAMS


def get_constraints() -> list:
    # The harness adds Not(And(get_constraints())); wrapping the combined region
    # in Not() here means it adds back exactly the region. Returning it as a
    # (non-empty) constraint also keeps the slice "usable" rather than a stub.
    return [
        Not(And((Int('self.ndim') == Int('mask.ndim')), Or((Int('self.ndim') <= IntVal(0)), (Select(Array('self.sizes', IntSort(), IntSort()), IntVal(0)) == Select(Array('mask.sizes', IntSort(), IntSort()), IntVal(0)))), Or((Int('self.ndim') <= IntVal(1)), (Select(Array('self.sizes', IntSort(), IntSort()), IntVal(1)) == Select(Array('mask.sizes', IntSort(), IntSort()), IntVal(1)))), Or((Int('self.ndim') <= IntVal(2)), (Select(Array('self.sizes', IntSort(), IntSort()), IntVal(2)) == Select(Array('mask.sizes', IntSort(), IntSort()), IntVal(2)))), Or((Int('self.ndim') <= IntVal(3)), (Select(Array('self.sizes', IntSort(), IntSort()), IntVal(3)) == Select(Array('mask.sizes', IntSort(), IntSort()), IntVal(3)))), Or((Int('self.ndim') <= IntVal(4)), (Select(Array('self.sizes', IntSort(), IntSort()), IntVal(4)) == Select(Array('mask.sizes', IntSort(), IntSort()), IntVal(4)))), (IntVal(11) == Int('mask.dtype')), Or((IntVal(5) == Int('self.dtype')), (IntVal(6) == Int('self.dtype')), (IntVal(7) == Int('self.dtype')), (IntVal(15) == Int('self.dtype'))), Or(Not(Bool('dim.present')), If((Int('self.ndim') == IntVal(0)), Or((Int('dim.val') == IntVal(0)), (Int('dim.val') == IntVal(-1))), And((Int('dim.val') >= (IntVal(-1) * Int('self.ndim'))), Not((Int('self.ndim') <= Int('dim.val')))))))),
    ]


def get_axioms() -> list:
    return []


def get_vars() -> dict:
    self = TensorVar('self')
    mask = TensorVar('mask')
    return {'self': self, 'mask': mask}


def build_solver() -> Solver:
    s = Solver()
    s.add(*get_axioms())
    return s
